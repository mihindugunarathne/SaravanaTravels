/**
 * Demo exports for `ImageAutoSlider` / `Component` (default Unsplash strip).
 * Use in a page during development, e.g. import { ImageAutoSliderDemoOne } from "@/components/ui/image-auto-slider-demo";
 */
import { Component } from "@/components/ui/image-auto-slider";

export function ImageAutoSliderDemoOne() {
  return <Component />;
}
